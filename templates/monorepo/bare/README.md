# {{projectName}}

> Monorepo scaffolded with **create-heildamm**

## Structure

```
apps/
  web/     — Next.js frontend
  api/     — Standalone API server
packages/
  ui/      — Shared design system
  types/   — Shared TypeScript interfaces
  config/  — Shared tsconfig / eslint
```

## Getting started

```bash
pnpm install
pnpm dev
```
